name: CI - App (MAUI)

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Install MAUI Android workload
        run: dotnet workload install maui-android

      - name: Build (Android)
        run: dotnet build app/HA.TFG.AppFinanzas.App.slnx -f net10.0-android -c Release

      - name: Test
        run: dotnet test app/HA.TFG.AppFinanzas.App.slnx -c Release --no-build --verbosity normal

  analyze:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Install MAUI Android workload
        run: dotnet workload install maui-android

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: csharp

      - name: Build for analysis
        run: dotnet build app/HA.TFG.AppFinanzas.App.slnx -f net10.0-android -c Release

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Install MAUI Windows workload
        run: dotnet workload install maui-windows

      - name: Build (Windows)
        run: dotnet build app/HA.TFG.AppFinanzas.App.slnx -f net10.0-windows10.0.19041.0 -c Release

      - name: Test
        run: dotnet test app/HA.TFG.AppFinanzas.App.slnx -c Release --no-build --verbosity normal
